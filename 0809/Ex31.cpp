#include <iostream> 

using namespace std;  

int main(){

    int saco = 0, porçao = 0, result = 0;

    cout  << "insira o peso do saco (Kg)" << endl;
    cin >> porçao*2;

    cout << "Insira a quantidade de racao que da a um dos gatos por dia (g)" << endl;
    cin >> saco;

    result= (saco*100)-(porçao)*5 ;

    cout << "Apos 5 dias, sobrara " << result << " g de comida." << endl;





}